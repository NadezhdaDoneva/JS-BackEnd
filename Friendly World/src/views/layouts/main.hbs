

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css" />
    <title>Home Page</title>
</head>

<body>

{{#if error}}
    <div>
        <div class="errorContainer">
            <p>{{error}}</p>
        </div>
    </div>
    {{/if}}

    <div id="wrapper">
        <header>
            <!-- Navigation -->
            <nav>
                <div>
                    <a href="/">Home</a>
                    <a href="/photos">Dashboard</a>
                    <a href="/search">Search</a>
                </div>

                <!-- Logged-in users -->
                {{#if isAuthenticated}}
                <div class="user">
                    <a href="/photos/create">Add Animal</a>
                    <a href="/users/logout">Logout</a>
                </div>
                {{else}}
                <!-- Guest users -->
                <div class="guest">
                    <a href="/users/login">Login</a>
                    <a href="/users/register">Register</a>
                </div>
                {{/if}}
            </nav>
        </header>
{{{body}}}

          <footer>
            <p>@Friendly World</p>
        </footer>
    </div>

</body>

</html>